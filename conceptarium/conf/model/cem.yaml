defaults:
  - _commons
  - _self_
  
_target_: "torch_concepts.nn.ConceptEmbeddingModel"

task_names: ${dataset.default_task_names}

training: "joint" # "joint", "independent"

embedding_size: 8

inference: 
  _target_: "torch_concepts.nn.DeterministicInference"
  _partial_: true