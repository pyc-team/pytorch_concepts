datamodule:
  batch_size: 512

  generation_seed: ${dataset.datamodule.seed}    # dataset generation seed
  
  n_gen: 10000 # number of samples to generate

  autoencoder_kwargs:
    noise: 0.
    latent_dim: 32
    lr: 0.0005
    epochs: 2000
    batch_size: 512
    patience: 50
