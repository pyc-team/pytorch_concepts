"""Factor types (internal). Public API: :mod:`torch_concepts.nn`.

The abstract :class:`ParametricFactor` interface and its two concrete kinds:
the directed :class:`ParametricCPD` and the undirected
:class:`ParametricPotential`. Depends only on the variable/distribution
primitives one level up.
"""
__all__: list[str] = []
